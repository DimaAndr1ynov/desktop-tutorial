# DOWNSHOT — лендинг фото/видеооператора (горные лыжи)

Одностраничный статичный сайт продажи услуг фото/видеооператора на горнолыжном склоне.
Владелец: DimaAndr1ynov. Язык сайта: русский.

**Живой сайт:** https://dimaandr1ynov.github.io/desktop-tutorial/

## Структура проекта

- `index.html` — весь сайт (инлайн CSS + JS, дизайн «DOWNSHOT»: тёмная тема, scroll-анимации, светящийся курсор)
- `assets/hero-ski.jpg` — фон hero-блока (слаломист, 2048×1152)
- `.github/workflows/deploy-pages.yml` — деплой на GitHub Pages при пуше в `main` (+ ручной запуск)
- `.github/workflows/fetch-hero-image.yml` — одноразовый ручной workflow: скачивает картинку по URL и коммитит в `assets/` (нужен потому, что из среды Claude Code внешние фото-хостинги заблокированы, а у раннеров GitHub доступ есть)

## Ключевые решения

- Заявки с формы уходят через mailto на **jimmyyobike@onet.pl** (в форме и в подписи «Или сразу на почту»)
- Тарифы: «Спуск» 1000 ₽ / «Заезд» 1500 ₽ (выделен) / «Съёмочный день» 3000 ₽
- Портфолио — пока заглушки-категории («скоро»), владелец добавит материал позже
- Фото в карточках услуг — хотлинки Unsplash (в песочнице Claude не грузятся, у посетителей работают)
- В hero-фоне прописан onerror-fallback на стоковое фото Unsplash

## Рабочий процесс (важно)

- Ветка разработки: `claude/photo-video-operator-site-fijp9n`; перед новой правкой пересоздавать её от `origin/main` (`git checkout -B <ветка> origin/main`), т.к. PR мержатся squash'ем
- Публикация: PR → squash-merge в `main` → деплой Pages запускается автоматически
- НО: коммиты, сделанные workflow-ботом (например, fetch-hero-image), деплой НЕ триггерят — после них запускать `deploy-pages.yml` вручную (workflow_dispatch)
- Репозиторий публичный (это требование бесплатного GitHub Pages)
- Проверка вёрстки: Playwright + Chromium `/opt/pw-browsers/chromium`, скриншоты; scroll-анимации на fullPage-скриншоте включать принудительно: `document.querySelectorAll('.reveal-group').forEach(g => g.classList.add('in-view'))`

## Открытые задачи

- [ ] Заменить фото в hero-карточке справа (сейчас Unsplash; владелец хотел фото команды в красных куртках — ждём прямую ссылку на файл или вложение-файл)
- [ ] Наполнить портфолио реальными фото/видео, когда владелец пришлёт материалы
- [ ] По желанию: заменить фон hero на собственный кадр владельца (текущий — стоковый снимок Red Bull, для коммерческого использования лучше свой)
